require 'spec_helper'

describe MessageSequence do
	it "should have a message sent from the admin with the boolean true"
	it "should have the user replied message content with the boolean set to false"
	it "the message should have a category id"
end 

